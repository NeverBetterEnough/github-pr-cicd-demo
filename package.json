{
  "name": "github-pr-cicd-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build.js",
    "check": "npm test && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
