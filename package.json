{
  "name": "github-pr-cicd-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build.js",
    "check": "npm test && npm run build",
    "predeploy": "node scripts/deploy/pre-deploy-check.js",
    "deploy:verify": "bash scripts/deploy/verify.sh",
    "health": "bash scripts/deploy/verify.sh"
  },
  "engines": {
    "node": ">=20"
  }
}
